[workspace]
members = [ "core", "quic", "http", "lib" ]
exclude = [ "quinn" ]
resolver = "3"
# edition = "2024"
# authors = ["samicpp"]

[profile.release]
panic = "abort"

[profile.dev]
panic = "unwind"
